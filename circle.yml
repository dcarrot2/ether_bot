machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --rm=false -t ether_bot .

test:
  override:
    - docker run -d ether_bot; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost/